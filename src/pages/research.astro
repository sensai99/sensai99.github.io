---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import { research } from '../data/content';
import microsoftLogo from '../layouts/temp/images/logos/microsoft.svg';
import umassLogo from '../layouts/temp/images/logos/umass.png';
import klaLogo from '../layouts/temp/images/logos/kla.svg';

const logos = {
  microsoft: microsoftLogo,
  umass: umassLogo,
  kla: klaLogo
} as const;
---

<BaseLayout title="Research | Sai Sreenivas Chintha">
  <Header />
  <main>
    <section class="section">
      <div class="section-header">
        <h2>RESEARCH</h2>
      </div>
      <div class="card timeline">
        {research.map((item) => (
          <article class="item">
            <div>
              <div class="role-row">
                <img
                  class="org-logo-img"
                  src={logos[item.logo as keyof typeof logos].src}
                  alt={`${item.org} logo`}
                />
                <h3>{item.role} · {item.org}</h3>
              </div>
              <ul>{item.bullets.map((bullet) => <li>{bullet}</li>)}</ul>
            </div>
            <span class="meta">{item.date}</span>
          </article>
        ))}
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  .timeline { display: grid; gap: 16px; }
  .role-row {
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .org-logo-img {
    width: 36px;
    height: 36px;
    object-fit: contain;
    flex-shrink: 0;
  }
</style>
